info:
  name: Listar Simulaciones
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/simulaciones"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
