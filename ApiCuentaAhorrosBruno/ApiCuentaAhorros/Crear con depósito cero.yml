info:
  name: Crear con depósito cero
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/simulaciones"
  body:
    type: json
    data: |-
      {
        "depositoInicial": 0,
        "tasaInteresAnual": 0.05,
        "plazoAnios": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
